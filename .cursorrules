# LibreMesh LiMeApp - Cursor IDE Rules

> **Contexto actualizado para Cursor IDE - Julio 2025**  
> **Estado del proyecto**: v4-foundation con arquitectura de documentación completa

## 🎯 Project Overview

LiMeApp es una aplicación web moderna construida con **Preact** para la gestión de routers LibreMesh. Proporciona una interfaz libre de complejidades técnicas para configuración y mantenimiento de nodos LibreMesh, con arquitectura de plugins que se comunica con servicios del router via llamadas ubus JSON-RPC.

### 🌟 Estado Actual del Proyecto

- **Rama activa**: `f/v4-foundation`
- **Documentación**: Sistema integral en español en `docs/dev-internal/`
- **Desarrollo colaborativo**: Framework IA+Humano completamente implementado
- **Testing**: Integración QEMU con 140+ tests y cobertura 85%+
- **Internacionalización**: 24 idiomas soportados (Español 100%, English 100%)

## 📚 Navegación de Documentación

### 🚀 Para desarrollo interno (team members):
- **[📖 Documentación Desarrollo](docs/dev-internal/README.md)** - Entrada única a toda la documentación
- **[⚡ Referencia Rápida](docs/dev-internal/00-quick-start/quick-reference.md)** - Comandos y workflows esenciales
- **[🛠️ Configuración Desarrollo](docs/dev-internal/00-quick-start/development-setup.md)** - Setup completo con IA+QEMU
- **[🤖 Guía Colaboración IA](docs/dev-internal/02-development/ai-collaboration.md)** - Workflows con Claude/Cursor/Copilot
- **[🖥️ Guía QEMU](docs/dev-internal/02-development/qemu-integration.md)** - Entorno testing LibreMesh auténtico

### 🌍 Para contribución upstream:
- **[📘 Tutorial](docs/Tutorial.md)** - Guía completa de desarrollo
- **[🛠️ Development Setup](DEVELOPMENT_SETUP.md)** - Configuración básica upstream-ready
- **[🤝 Contributing](CONTRIBUTING.md)** - Proceso de contribución

## 🏗️ Development Framework
- **Frontend**: Preact (React-like framework, 3kB bundle)
- **TypeScript**: Full type safety with path aliases configured
- **State Management**: TanStack Query (modern) + Redux (legacy)
- **Testing**: Jest + Testing Library with QEMU integration
- **Styling**: CSS Modules + Tailwind CSS + Bootstrap-inspired global classes
- **i18n**: LinguiJS with 24+ language support
- **Build**: Webpack with development proxy to LibreMesh routers

## Standardized QEMU Workflow (2025)

### One-Command Deployment ⭐
```bash
npm run deploy:qemu        # Build + deploy to running QEMU (recommended)
```

### QEMU Management
```bash
npm run qemu:start         # Start QEMU LibreMesh cleanly
npm run qemu:stop          # Stop QEMU processes properly  
npm run qemu:restart       # Clean restart (stop + start)
npm run qemu:status        # Check QEMU and lime-app status
```

### 🛠️ Development Commands

#### Setup y Dependencias
```bash
npm install                     # Instalar dependencias del proyecto
npm run verify:setup           # ⭐ Verificación completa del entorno
npm run verify:qemu             # Verificar entorno QEMU LibreMesh
npm run verify:ai               # Verificar herramientas IA
```

#### Servidor de Desarrollo
```bash
# Desarrollo básico (frontend + mocks)
npm run dev                     # Servidor con hot-reload (proxies a 10.13.0.1)

# Desarrollo avanzado (con backend real)
npm run qemu:start              # Iniciar LibreMesh en QEMU
npm run qemu:dev                # Servidor desarrollo con backend QEMU real
npm run deploy:qemu             # ⭐ Build + deploy a QEMU (comando único)

# Desarrollo con router personalizado
env NODE_HOST=192.168.1.1 npm run dev
```

#### Build Commands
```bash
npm run build                   # Build de desarrollo
npm run build:production        # Build de producción (incluye compilación i18n)
npm run serve                   # Build y servir versión de producción
```

#### Testing Comprehensivo
```bash
# Testing básico
npm test                        # Ejecutar todos los tests
npm run test -- --watch         # Modo watch para desarrollo
npm run test -- --coverage      # Tests con reporte de cobertura

# Testing específico
npm run test plugins/lime-plugin-name/  # Tests de plugin específico
npm run test:authenticated      # Tests autenticados contra QEMU real
npm run test:integration        # Tests de integración completos

# Limpieza de cache
npm run clear-jest              # Limpiar cache de Jest
```

#### Quality Assurance
```bash
# QA rápido (desarrollo iterativo)
npm run qa:fast                 # Lint + tests básicos

# QA completo (pre-commit)
npm run qa:full                 # Tests + lint + build + integración

# QA con IA (análisis avanzado)
npm run qa:ai                   # Review IA + security + documentación

# QA multiplataforma
npm run qa:cross-platform       # Tests en múltiples entornos

# QA para upstream
npm run qa:upstream             # Preparación para contribución upstream
```

## Code Patterns & Conventions

### Plugin Development Structure
```
plugins/lime-plugin-<name>/
├── index.ts                    # Plugin registration { name, page, menu }
├── <name>.spec.js             # Component tests
├── <name>.stories.js          # Storybook stories
└── src/
    ├── <name>Page.js          # Main component
    ├── <name>Api.js           # Backend endpoints
    ├── <name>Queries.js       # TanStack Query hooks
    └── style.less             # Component styles
```

### Component Development Pattern (TDD)
1. Write component tests with mocked APIs using `render()` from `utils/test_utils`
2. Implement component to pass tests
3. Write API endpoint tests with proper mock cleanup
4. Implement API endpoints following ubus JSON-RPC pattern
5. Create Storybook stories for component states

### Path Aliases (Configured)
```javascript
~/ → src/
components/ → src/components/
containers/ → src/containers/
utils/ → src/utils/
plugins/ → plugins/
```

### Backend Communication
- **uHTTPd client**: Singleton service for ubus JSON-RPC calls
- **Default router IP**: `10.13.0.1` (override with `NODE_HOST` env var)
- **API endpoints**: Functions that define request URLs and payloads
- **Queries**: Read operations using `useQuery`
- **Mutations**: Write operations using `useMutation`

## Best Practices for Cursor

### When Writing Components
- Use `Fragment` from `preact` instead of React fragments (`<>`)
- Import path aliases: `import { Component } from '~/components'`
- Follow existing styling patterns (CSS Modules + global classes)
- Include comprehensive PropTypes or TypeScript interfaces

### When Writing Tests
- Mock API calls using `jest.mock('./src/<name>Api')`
- Clean up query cache: `act(() => queryCache.clear())`
- Use `render()` from `utils/test_utils` for consistent provider setup
- Test user interactions with `@testing-library/preact`
- Use `screen.findBy*` async queries for UI state changes

### When Working with APIs
- Follow ubus JSON-RPC protocol patterns
- Use TanStack Query for data fetching (preferred over Redux)
- Handle session authentication with username/password
- Test endpoints with QEMU integration for real router testing

### File Creation Guidelines
- **ALWAYS prefer editing existing files** over creating new ones
- **NEVER create documentation files** unless explicitly requested
- Follow plugin structure conventions for new plugins
- Use `npm run create-plugin <name>` for bootstrapping

## 🤖 Human-AI Collaborative Development

Este proyecto soporta **desarrollo colaborativo humano-IA** across múltiples plataformas y herramientas AI.

### 🚀 Quick Start para Colaboración IA
```bash
# Verificar integración herramientas IA
npm run verify:ai

# Iniciar desarrollo con verificación de entorno
npm run dev:start

# Verificaciones de calidad asistidas por IA
npm run qa:ai

# Review de código con IA
npm run ai:review
```

### 🎯 Mejores Prácticas IA para Cursor

**Cursor IDE Specialization:**
- **Completado código real-time**: Aprovecha `Ctrl+L` para chat contextual
- **Refactoring inline**: `Ctrl+K` para natural language to code
- **Exploración código**: `Ctrl+Shift+L` para análisis de archivos
- **Prototipado rápido**: Templates y snippets inteligentes

**Patterns efectivos:**
- Describe la intención antes de escribir código
- Usa comentarios como prompts para generación
- Aprovecha el contexto del editor para sugerencias precisas
- Combina con Claude Code para debugging complejo

### 🔧 AI Quality Gates
```bash
npm run verify:setup         # Development environment verification
npm run qa:ai                # AI-assisted quality checks
npm run qa:upstream          # Upstream contribution readiness
npm run ai:review            # Code review con sugerencias IA
npm run ai:security          # Escaneo vulnerabilidades seguridad IA
npm run ai:docs              # Verificación completeness documentación IA
```

## LibreMesh Integration

### QEMU LibreMesh Environment
- **Recommended image**: `libremesh-2020.4-ow19-x86-64` (stable, tested)
- **Network**: QEMU creates LibreMesh node at `10.13.0.1`
- **Access**: Console via `screen -r libremesh`, web at `http://10.13.0.1`
- **Prerequisites**: `../lime-packages/` directory with QEMU tools

### Real Router Configuration
- Production deployments serve from `/www/app/` via uHTTPd
- Access via `thisnode.info` or router IP address
- Real configuration manipulation through tested API endpoints
- Session-based authentication with ACL permissions

## Common Issues & Solutions

### Frontend-Only Development
Without LibreMesh backend, expect:
- `/ubus` endpoint 500 errors (normal - no router backend)
- WebSocket connection failures (hot-reload limitation)
- JSON parsing errors from API calls (HTML error pages instead of JSON)

### I18n Integration
- Ensure `i18n.activate()` called before rendering I18nProvider
- Use `lingui extract` → `lingui compile` workflow
- 24 languages supported, Spanish at 100% translation

### VS Code Integration
- F5 debugging configured with 8 launch configurations
- Integrated terminal has QEMU environment variables
- Jest test explorer enabled with coverage on load
- File nesting patterns for cleaner explorer view

## 🎯 Cursor-Specific Development Patterns

### 🚀 Effective Cursor Usage

**Code Generation:**
- Use `Ctrl+K` with specific, actionable prompts
- Describe component behavior before implementation
- Leverage file context for accurate suggestions
- Combine with existing code patterns for consistency

**Debugging with Cursor:**
- Use `Ctrl+L` for contextual debugging help
- Share error messages with surrounding code context
- Ask for step-by-step debugging approaches
- Request specific test cases for issue reproduction

**Refactoring Patterns:**
- Select code blocks and use `Ctrl+K` for transformation
- Ask for TypeScript interface improvements
- Request performance optimizations with explanations
- Get suggestions for modern React patterns

### 📝 Cursor Prompt Templates

**For Component Creation:**
```
Create a Preact component for [functionality] that:
- Uses TypeScript with proper interfaces
- Follows our CSS Modules pattern
- Includes comprehensive PropTypes
- Has proper error handling
- Is testable with our test utilities
```

**For API Integration:**
```
Implement ubus JSON-RPC endpoint for [feature] that:
- Follows our API patterns in [similar file]
- Uses TanStack Query for state management
- Includes proper error handling
- Has comprehensive tests
- Supports our authentication flow
```

## 🏆 Contribution Standards

### 📝 Formato Commit Message para Asistencia IA
```bash
git commit -m "feat(component): add mesh node status visualization

- Implemented TypeScript React component with real-time updates
- Added comprehensive test suite with 95% coverage
- Created Storybook stories for all component states

🤖 AI-assisted with: Cursor for component structure
🤖 AI-assisted with: Claude Code for testing strategy"
```

### 🌐 Upstream Preparation
- Use `npm run qa:upstream` for contribution readiness
- Follow LibreMesh project standards and conventions
- Maintain multilingual documentation capabilities
- Ensure cross-platform compatibility (Linux, WSL2, macOS)
- Check `.upstream-exclude` for internal-only files

### 🧠 Development Wisdom & Best Practices

**Framework Desarrollo Humano-IA Colaborativo**
- Framework clase producción: Integración Claude Code + Cursor + GitHub Copilot validada
- Especialización herramientas IA: Claude Code (debugging/arquitectura), Cursor (integración IDE), GitHub Copilot (completion)
- Compatibilidad multiplataforma: Windows WSL2, Linux, macOS alcanzable con tooling verificación apropiado

**Context Management Best Practices**
- **Memoria proyecto**: Mantener CLAUDE.md como base conocimiento persistente IA
- **Organización clara**: Estructura archivos afecta comprensión IA dramáticamente
- **Feedback loops**: Scripts verificación proporcionan IA con validación calidad
- **Task tracking**: Listas todo esenciales para coordinación proyecto complejo